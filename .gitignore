# Only media/ videos + the poster script + state are allowed.
# Block everything else by default (keeps secrets out of this public repo).
*
!.gitignore
!README.md
!post_retrobyte_social.py
!.retrobyte_post_state.json
!media/
!media/**
